export interface WeatherResponse {
    id: number;
    applicable_date: string;
    min_temp: number;
    max_temp: number;
}

export interface Weather {
    id: number;
    applicableDate: string;
    minTemp: number;
    maxTemp: number;
}
